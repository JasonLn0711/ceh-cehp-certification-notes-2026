# M14 — Web Applications — original practice v1.0.0

[Bank and modes](README.md) · [Current route](../../study-plan/uuu-aligned-ceh-cehp-2026-09-18.md)

30 original items. Start with Q001–Q010, then continue to Q030. Teaching: one at a time. Drill: ten at a time, with choice and confidence before the key.

All scenarios are original educational cases. Any test activity is limited to an authorized lab. These are not official or recalled exam questions. Do not open the key before committing the corresponding answers.

### CEH26-M14-Q001

An unsafely rendered comment runs script when another user views it. Which weakness fits?

A. Broken object authorization
B. Server-side request forgery
C. Cross-site scripting
D. OS command injection

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q002

A forum saves a comment that executes whenever readers open the thread. Which XSS delivery pattern fits?

A. Reflected XSS
B. DOM-based XSS
C. Stored XSS
D. Context-appropriate output encoding

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q003

A user changes /orders/41 to /orders/42 and reads another user's order. Which check is missing?

A. Deny-by-default access policy
B. Object-level authorization
C. Function-level authorization
D. Tenant isolation

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q004

An API expects a positive integer but accepts arbitrary strings and unbounded values. Which input control fits?

A. Parameterized interpreter use
B. Canonical path containment
C. Safe file storage
D. Allowlisted input validation

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q005

A browser blocks a script from reading a cross-origin response, but a direct HTTP client can still call the API. Which policy explains the browser behavior?

A. Server-side validation
B. Content Security Policy
C. Business-logic validation
D. CORS policy

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q006

A route works for an administrator, but nobody has tried the same operation as an ordinary user. Which test coverage is missing?

A. Safe proof of impact
B. Positive and negative authorization tests
C. State-change verification
D. Regression coverage

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q007

Changing an invoice identifier reveals another customer's invoice despite a valid login. Which weakness fits?

A. Broken object authorization
B. Cross-site scripting
C. Server-side request forgery
D. OS command injection

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q008

A search term is immediately echoed into unsafe HTML in the response. Which XSS pattern fits?

A. DOM-based XSS
B. Reflected XSS
C. Context-appropriate output encoding
D. Stored XSS

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q009

A hidden administrator button is removed, but a normal user can still call its backend action. Which check is missing?

A. Function-level authorization
B. Tenant isolation
C. Object-level authorization
D. Deny-by-default access policy

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q010

A file upload should not let a user choose an executable server path. Which storage control is needed?

A. Safe file storage
B. Parameterized interpreter use
C. Allowlisted input validation
D. Canonical path containment

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q011

A site limits which script sources the browser may execute. Which defense-in-depth mechanism fits?

A. Business-logic validation
B. Content Security Policy
C. CORS policy
D. Server-side validation

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q012

An API returns 200, yet the requested update might have been ignored. Which verification is required?

A. State-change verification
B. Regression coverage
C. Safe proof of impact
D. Positive and negative authorization tests

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q013

A URL-fetch feature can be directed to an internal-only service from the server. Which weakness fits?

A. OS command injection
B. Cross-site scripting
C. Server-side request forgery
D. Broken object authorization

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q014

Client-side code reads a URL fragment and passes it to an unsafe HTML sink without a server round trip. Which pattern fits?

A. DOM-based XSS
B. Reflected XSS
C. Stored XSS
D. Context-appropriate output encoding

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q015

A query forgets the organization constraint and returns records from another customer organization. Which boundary failed?

A. Object-level authorization
B. Deny-by-default access policy
C. Tenant isolation
D. Function-level authorization

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q016

A shell wrapper treats a filename as part of command syntax. Which design change separates the data from execution structure?

A. Parameterized interpreter use
B. Allowlisted input validation
C. Safe file storage
D. Canonical path containment

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q017

A disabled form field can be changed by sending a custom request. Where must the important constraint be enforced?

A. Content Security Policy
B. CORS policy
C. Server-side validation
D. Business-logic validation

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q018

A tester can establish an object-access flaw using one synthetic record rather than downloading customer data. Which evidence principle applies?

A. State-change verification
B. Safe proof of impact
C. Regression coverage
D. Positive and negative authorization tests

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q019

A diagnostic form concatenates input into a shell command and allows unintended shell interpretation. Which weakness fits?

A. Server-side request forgery
B. Broken object authorization
C. Cross-site scripting
D. OS command injection

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q020

A template displays an untrusted name in an HTML text node while preserving it as text. Which defense is appropriate?

A. Reflected XSS
B. DOM-based XSS
C. Stored XSS
D. Context-appropriate output encoding

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q021

A newly added route has no permission declaration and becomes accessible automatically. Which policy would avoid that default?

A. Function-level authorization
B. Tenant isolation
C. Deny-by-default access policy
D. Object-level authorization

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q022

A path filter checks raw text but misses an equivalent encoded traversal. Which final-target check is needed?

A. Parameterized interpreter use
B. Safe file storage
C. Allowlisted input validation
D. Canonical path containment

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q023

A user redeems the same single-use coupon twice through a valid-looking request sequence. Which validation is needed?

A. Business-logic validation
B. Content Security Policy
C. Server-side validation
D. CORS policy

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q024

A fix works today, but the development team needs a check that detects its return. Which testing practice fits?

A. State-change verification
B. Safe proof of impact
C. Positive and negative authorization tests
D. Regression coverage

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q025

A browser executes data that the application intended to display as text. Which boundary has failed?

A. OS command injection
B. Server-side request forgery
C. Cross-site scripting
D. Broken object authorization

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q026

The vulnerable path exists entirely in browser-side data flow from a source to a script-capable sink. Which classification fits?

A. Reflected XSS
B. DOM-based XSS
C. Context-appropriate output encoding
D. Stored XSS

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q027

A normal account submits a direct request to a privileged export function. Which authorization layer must reject it?

A. Function-level authorization
B. Tenant isolation
C. Deny-by-default access policy
D. Object-level authorization

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q028

Renaming an uploaded file is insufficient if its storage directory still executes active content. Which broader design must be applied?

A. Allowlisted input validation
B. Safe file storage
C. Parameterized interpreter use
D. Canonical path containment

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q029

Reflecting any requesting origin alongside credential permission creates a dangerous browser read policy. Which mechanism is misconfigured?

A. Content Security Policy
B. Business-logic validation
C. CORS policy
D. Server-side validation

Choice: ___  Confidence: low / medium / high

### CEH26-M14-Q030

Only forbidden requests are tested, so a fix could deny everyone and still appear successful. Which coverage should be added?

A. Safe proof of impact
B. State-change verification
C. Regression coverage
D. Positive and negative authorization tests

Choice: ___  Confidence: low / medium / high
