# M15 — SQL Injection — original practice v1.0.0

[Bank and modes](README.md) · [Current route](../../study-plan/uuu-aligned-ceh-cehp-2026-09-18.md)

30 original items. Start with Q001–Q010, then continue to Q030. Teaching: one at a time. Drill: ten at a time, with choice and confidence before the key.

All scenarios are original educational cases. Any test activity is limited to an authorized lab. These are not official or recalled exam questions. Do not open the key before committing the corresponding answers.

### CEH26-M15-Q001

A lab response includes an additional compatible result set appended to the original query output. Which injection technique fits?

A. Boolean-based blind SQL injection
B. Union-based SQL injection
C. Time-based blind SQL injection
D. Error-based SQL injection

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q002

User text is inserted directly into the SQL statement before execution. Which root cause is present?

A. Data interpreted as query syntax
B. Least-privilege database identity
C. Bound parameters
D. Allowlisted structural choices

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q003

A team claims immunity because all queries are inside procedures, but those procedures concatenate user text. Which review is needed?

A. Input escaping limitation
B. Client-side check limitation
C. WAF defense-in-depth limitation
D. Stored procedure review

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q004

A test expression valid in one database is rejected by another engine. Which principle explains the difference?

A. Timing uncertainty
B. Query execution evidence
C. Database-specific behavior
D. Error disclosure

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q005

A test account retrieves another customer's private record through unsafe query handling. Which impact is demonstrated?

A. Availability impact
B. Unauthorized modification
C. Authentication bypass
D. Unauthorized read

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q006

The original lab payload now returns no matching literal username and does not broaden the result. Which check succeeded?

A. Test literal handling
B. Review every query path
C. Retest valid inputs
D. Check database permissions

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q007

A crafted expression causes database errors that disclose useful query information. Which technique fits?

A. Boolean-based blind SQL injection
B. Time-based blind SQL injection
C. Error-based SQL injection
D. Union-based SQL injection

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q008

A query uses placeholders and supplies the username as a separate parameter value. Which defense is used?

A. Least-privilege database identity
B. Allowlisted structural choices
C. Data interpreted as query syntax
D. Bound parameters

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q009

A custom quote-replacement routine behaves differently after an encoding change. Which defensive limitation is shown?

A. Input escaping limitation
B. Client-side check limitation
C. Stored procedure review
D. WAF defense-in-depth limitation

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q010

A page changes after unusual input, but the change may come from application validation rather than SQL. What evidence is still needed?

A. Database-specific behavior
B. Query execution evidence
C. Error disclosure
D. Timing uncertainty

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q011

An authorized lab input changes a protected row that the user should not edit. Which impact is demonstrated?

A. Authentication bypass
B. Unauthorized read
C. Unauthorized modification
D. Availability impact

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q012

A patch prevents injection but rejects ordinary valid customer searches. Which required check failed?

A. Check database permissions
B. Retest valid inputs
C. Review every query path
D. Test literal handling

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q013

Two otherwise similar inputs change the page according to whether a database condition is true or false. Which technique fits?

A. Time-based blind SQL injection
B. Error-based SQL injection
C. Union-based SQL injection
D. Boolean-based blind SQL injection

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q014

A user chooses a sort column, which the API cannot treat as a normal value parameter. Which design should constrain it?

A. Data interpreted as query syntax
B. Allowlisted structural choices
C. Least-privilege database identity
D. Bound parameters

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q015

A JavaScript filter rejects punctuation, but a direct request reaches the SQL query unchanged. Which limitation applies?

A. Stored procedure review
B. Input escaping limitation
C. WAF defense-in-depth limitation
D. Client-side check limitation

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q016

A single request is slow during a busy period. Which limitation prevents confidently declaring time-based injection?

A. Query execution evidence
B. Error disclosure
C. Timing uncertainty
D. Database-specific behavior

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q017

The application accepts a login without a valid password because the SQL predicate is changed. Which impact fits?

A. Availability impact
B. Authentication bypass
C. Unauthorized modification
D. Unauthorized read

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q018

The main endpoint is repaired while a legacy export route still concatenates the same input. Which review was incomplete?

A. Test literal handling
B. Check database permissions
C. Retest valid inputs
D. Review every query path

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q019

A controlled test causes a repeatable conditional response delay without visible database output. Which technique fits?

A. Time-based blind SQL injection
B. Union-based SQL injection
C. Error-based SQL injection
D. Boolean-based blind SQL injection

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q020

A reporting application can drop unrelated tables despite needing only SELECT access. Which containment control is missing?

A. Least-privilege database identity
B. Bound parameters
C. Allowlisted structural choices
D. Data interpreted as query syntax

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q021

A traffic rule blocks a known payload while the vulnerable code remains unchanged. Which limitation should be recorded?

A. Input escaping limitation
B. Stored procedure review
C. WAF defense-in-depth limitation
D. Client-side check limitation

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q022

The server exposes a table name and SQL fragment in an error. Which information issue is directly established?

A. Query execution evidence
B. Timing uncertainty
C. Error disclosure
D. Database-specific behavior

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q023

A query consumes the database's resources until valid requests fail. Which impact fits?

A. Availability impact
B. Unauthorized modification
C. Authentication bypass
D. Unauthorized read

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q024

The fixed application still connects as a database superuser. Which separate control needs attention?

A. Review every query path
B. Check database permissions
C. Retest valid inputs
D. Test literal handling

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q025

The application suppresses errors and data, but a condition reliably changes whether a result indicator appears. Which blind technique applies?

A. Union-based SQL injection
B. Boolean-based blind SQL injection
C. Time-based blind SQL injection
D. Error-based SQL injection

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q026

An input containing SQL punctuation is treated literally as an account name. Which mechanism should produce that result?

A. Bound parameters
B. Data interpreted as query syntax
C. Allowlisted structural choices
D. Least-privilege database identity

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q027

A procedure uses parameterized calls at its outer interface but assembles unsafe SQL internally. Which boundary still needs examination?

A. Input escaping limitation
B. Stored procedure review
C. WAF defense-in-depth limitation
D. Client-side check limitation

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q028

A report labels the backend solely from one ambiguous error string. Which implementation-context check should strengthen the conclusion?

A. Query execution evidence
B. Timing uncertainty
C. Error disclosure
D. Database-specific behavior

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q029

A UNION result reveals fields outside the approved result set without changing any row. Which impact is directly supported?

A. Availability impact
B. Authentication bypass
C. Unauthorized modification
D. Unauthorized read

Choice: ___  Confidence: low / medium / high

### CEH26-M15-Q030

A safe insert stores text that an unsafe scheduled report later concatenates into SQL. Which coverage principle finds this second-order path?

A. Test literal handling
B. Retest valid inputs
C. Review every query path
D. Check database permissions

Choice: ___  Confidence: low / medium / high
